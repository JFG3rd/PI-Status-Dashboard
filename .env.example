# Dashboard network configuration (override as needed)
# Set only if auto-detect is wrong.
DASHBOARD_IP_OVERRIDE=
NETWORK_INTERFACE_PRIORITY=eth0,end0,wlan0

# Backup defaults
NVME_BACKUP_PATH=/nvme-backups
USB_BACKUP_PATH=/mnt/backup-ssd/backups
BACKUP_DEFAULT_PATH=/nvme-backups

# Timezone
TZ=Europe/Berlin
